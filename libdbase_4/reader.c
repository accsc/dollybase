#include <stdio.h>

/* Read a line max 1 Kb lengh from a open file */
/*
char *read_line(FILE *b)
{
int i;
char pes[1024];
for(i = 0; i<=1024; ++i)
{
pes[i] = getc(b);
if( pes[i] == '\n')
break;
}
pes[i] = 0;
return pes;
}
*/
